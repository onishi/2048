# CLAUDE.md

このリポジトリで作業する AI エージェント向けの注意事項。

## 開発計画の進捗管理

- 開発計画は `PLAN.md` に記載されている（`SPEC.md` を元に、フェーズごとの実装タスクへ分解したもの）。
- `PLAN.md` の各タスクはチェックボックス（`- [ ]`）形式になっている。
- タスクの実装が完了したら、そのタイミングで該当するチェックボックスを `- [x]` に更新すること。

## PR作成の必須化

- `main` ブランチへの直接 push は禁止。コードの変更は必ず作業用ブランチで行い、Pull Request を作成した上でマージすること。
- ドキュメントのみの変更であっても同様に PR を経由する。
- 変更を加えたら、コミット・push・PR作成までを一つの作業として完了させること（PR作成を省略しない）。
